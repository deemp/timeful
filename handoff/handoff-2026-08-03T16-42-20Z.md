# Session Handoff

- Created session: `ses_037822487ffe3wTgKkITiRvU7m`

## Important Decisions

- Refactor `ScheduleOverlap.vue` sequentially with one agent; do not split the implementation among parallel agents.
- Keep `ScheduleOverlap.vue` as the public component and composition root. Do not add a wrapper-only layout component.
- Preserve the existing child-component view-model contracts and the public API declared in `frontend/src/composables/event/types.ts`.

## Findings And Risks

- `frontend/src/components/schedule_overlap/ScheduleOverlap.vue` is 1,960 lines and combines feature setup, timed-grid presentation, grid interaction/tooltip behavior, view-model assembly, and guest-response workflows.
- The visual tree is already split into `ScheduleOverlapTimeGrid.vue`, `ScheduleOverlapDaysOnlyGrid.vue`, `ScheduleOverlapSidebar.vue`, `ScheduleOverlapMobileOverlay.vue`, and `ToolRow.vue`; the primary refactor target is orchestration rather than additional Vue presentation components.
- `ScheduleOverlap.test.ts` reaches into `wrapper.vm` for internal state such as `renderedRows` and `saveGuestName`. Extracted behavior should receive focused tests, leaving the component test as integration coverage.

## Validation

- Ran `wc -l frontend/src/components/schedule_overlap/* frontend/src/composables/schedule_overlap/*.ts | sort -n`; `ScheduleOverlap.vue` is 1,960 lines and `ScheduleOverlap.test.ts` is 2,169 lines.
- No code changes or frontend test/build commands were run.

## Remaining Work

1. Extract pure page-slot, collapse-span, and rendered-row logic into `frontend/src/components/schedule_overlap/scheduleOverlapGridRows.ts`, with focused tests.
2. Extract reactive timed-grid presentation into `useTimedGridPresentation.ts`: collapse expansion state, rendered rows, scheduled-event fragments, availability overlays, and time-grid style/event arrays.
3. Extract tooltip and timed-grid interaction adapters into `useTimedGridInteractions.ts`.
4. Extract guest edit, delete, rename, and editing lifecycle workflows into `useGuestAvailabilityActions.ts`.
5. Simplify `useScheduleOverlapViewModels.ts` inputs after the real domains are extracted, while keeping its output interfaces explicit.
6. Reduce `ScheduleOverlap.vue` to props/emits, domain-composable wiring, child view models/listeners, and `defineExpose`.
7. Run the required frontend checks: `npm run lint`, `npm run typecheck`, `npm run build`, and `npm run test:unit` from `frontend/`.

## Relevant Files

- `frontend/src/components/schedule_overlap/ScheduleOverlap.vue`
- `frontend/src/components/schedule_overlap/ScheduleOverlap.test.ts`
- `frontend/src/components/schedule_overlap/useScheduleOverlapViewModels.ts`
- `frontend/src/components/schedule_overlap/scheduleOverlapViewModels.ts`
- `frontend/src/components/schedule_overlap/useScheduleOverlapController.ts`
- `frontend/src/components/schedule_overlap/ScheduleOverlapTimeGrid.vue`
- `frontend/src/components/schedule_overlap/ScheduleOverlapGridDragBinding.test.ts`
- `frontend/src/components/schedule_overlap/scheduleOverlapRendering.ts`
- `frontend/src/composables/event/types.ts`
- `frontend/src/views/Event.vue`
- `frontend/src/components/landing/LandingPageCalendar.vue`
